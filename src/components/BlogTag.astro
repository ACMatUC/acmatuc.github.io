---
interface Props {
  tag: string;
}
const { tag } = Astro.props;
---

<a
  href={`/blog/tags/${tag}`}
  class="!text-white bg-neutral-600 hover:bg-red-500 transition-colors py-1.5 px-2 font-display uppercase text-xs font-medium rounded-md not-last:mr-2"
  >{tag}</a
>
