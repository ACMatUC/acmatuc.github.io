---
import { getCollection } from "astro:content";

import acmWideTransparent from "../assets/acm-wide-transparent.png";
import acmWideTransparentBlack from "../assets/acm-wide-transparent-black.png";
import { Image } from "astro:assets";

const allClasses = await getCollection("members");
---

<nav class="bg-neutral-800 flex justify-between h-16 px-2">
  <span class="flex">
    <a
      href="/"
      class="w-max hover:bg-red transition-colors"
    >
      <Image
        src={acmWideTransparent}
        alt="ACM@UC Logo"
        class="h-16 w-auto hover:opacity-0 transition-opacity"
      />
      <Image
        src={acmWideTransparentBlack}
        alt="ACM@UC Logo"
        class="absolute top-0 h-16 w-auto opacity-0 hover:opacity-100 transition-opacity"
      />
    </a>
    <menu class="flex gap-1">
      {
        [
          ["Resources", "/resources"],
          ["Executives", "/executives"],
          ["Members", "/members"],
          ["Blog", "/blog"],
        ].map(([title, url]) => (
          <li class="contents">
            <a
              href={url}
              class="font-bold h-16 px-3 uppercase font-display flex justify-center items-center hover:bg-red transition-colors"
            >
              <span class="text-white">{title}</span>
            </a>
          </li>
        ))
      }
    </menu>
  </span>
  <ul class="flex gap-1">
    <a
      target="_blank"
      rel="noopener"
      href="https://instagram.com/acmatuc"
      class="h-16 w-16 grid place-items-center hover:bg-instagram transition-colors"
    >
      <img
        src="/assets/instagram.svg"
        alt="instagram logo"
        class="h-5 w-auto"
      />
    </a>
    <a
      target="_blank"
      rel="noopener"
      href="https://discord.gg/6aYBT6s"
      class="h-16 w-16 grid place-items-center hover:bg-discord transition-colors"
    >
      <img
        src="/assets/discord.svg"
        alt="discord logo"
        class="h-5 w-auto"
      />
    </a>
    <a
      target="_blank"
      rel="noopener"
      href="https://x.com/ACMatUCorg"
      class="h-16 w-16 grid place-items-center hover:bg-white transition-all group"
    >
      <img
        src="/assets/x.svg"
        alt="x logo"
        class="h-5 w-auto transition-all group-hover:invert"
      />
    </a>
    <a
      target="_blank"
      rel="noopener"
      href="https://linkedin.com/company/acm-uc"
      class="h-16 w-16 grid place-items-center hover:bg-linkedin transition-colors"
    >
      <img
        src="/assets/linkedin.svg"
        alt="linkedin logo"
        class="h-5 w-auto"
      />
    </a>
  </ul>
</nav>

<style lang="scss">
  /*@use "../styles/theme.scss" as theme;

  a {
    font-family: theme.$font-display;
  }

  nav {
    background-color: theme.$gray-850;
    display: flex;
    justify-content: space-between;
    height: 4rem;

    img {
      height: 4rem;
    }
    img.hover {
      position: absolute;
      top: 0;
      left: 0;
      opacity: 0;
      transition: opacity 100ms linear;
    }

    span {
      display: flex;

      > a {
        margin-left: 0.5rem;
        position: relative;
        display: block;
        transition: background 100ms linear;
      }
      > a:hover {
        background-color: theme.$red-500;

        img {
          opacity: 100;
        }
      }
    }
  }
  .links {
    display: flex;
    margin: 0;
    padding: 0;

    > li,
    > * > li {
      height: 4rem;
      padding: 0 0.8rem;
      display: flex;
      justify-content: center;
      align-items: center;
      color: theme.$gray-100 !important;
      transition: background 100ms linear;

      a {
        margin-left: 8px;
        color: inherit;
        text-decoration: none;
        text-transform: uppercase;
        font-weight: bold;
      }
      a:last-child {
        margin-right: 8px;
      }
      img {
        height: 1.5rem;
        margin-left: 0.3rem;
      }
    }
    > li:hover,
    > *:hover > li {
      background-color: theme.$red-500;
    }

    li::marker {
      content: none;
    }
    .dropdown {
      position: relative;

      menu {
        display: flex;
        pointer-events: none;
        opacity: 0;
        position: absolute;
        top: 4rem;
        left: 0;
        padding: 0.5rem 0;
        margin: 0;
        flex-direction: column;
        background-color: theme.$red-500;
        transition: opacity 100ms linear;

        li {
          color: theme.$white;
          font-family: theme.$font-display;
          text-transform: uppercase;
          font-weight: 700;
          text-wrap: nowrap;
          padding: 0.5rem 1.7rem 0.5rem 1.5rem;
          transition:
            background 100ms linear,
            color 100ms linear;
        }
        li:hover {
          background-color: theme.$red-100;
          color: theme.$red-500;
        }
        a {
          color: inherit;
          text-decoration: none;
        }
      }
    }
    .dropdown:hover menu {
      opacity: 100;
      pointer-events: all;
    }
  }

  .socials {
    display: flex;
    padding: 0;
    margin: 0;
    padding-right: 0.5rem;

    a {
      height: 4rem;
      width: 4rem;
      display: grid;
      place-items: center;
      transition: background 100ms linear;
      margin: 0;
    }
    a:hover.instagram {
      background-color: #ff0069;
    }
    a:hover.discord {
      background-color: #5865f2;
    }
    a:hover.x {
      background-color: #fff;

      img {
        filter: invert(1);
      }
    }
    a:hover.linkedin {
      background-color: #0072b1;
    }
    img {
      height: 1.2rem;
      stroke: white;
      transition: filter 100ms linear;
    }
  }*/
</style>
